/**
* This client forwards messages from GCS to the veihice and vice versa. The GCS needs to use a udpm
* URL while the vehicle needs to use IPC. This client is responsible for forwarding messages between
* the two transports. NOTE: GCS needs the modified ZCM found in gcs/zcm-modified to run, but the
* vehicle does not.
*
* @author Romario Pashollari (rpash@umich.edu)
*/
